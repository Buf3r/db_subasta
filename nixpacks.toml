[variables]
    PHP_VERSION = '8.2'

[phases.setup]
    nixPkgs = ['php82', 'php82Packages.composer', 'php82Packages.intl', 'apache']

[phases.build]
    cmds = ['composer install --no-dev --optimize-autoloader']

[start]
    cmd = 'php spark serve --host 0.0.0.0 --port $PORT'